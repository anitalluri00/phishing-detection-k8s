apiVersion: v1
kind: Service
metadata:
  name: phishing-service
  namespace: phishing-detection
spec:
  type: LoadBalancer
  selector:
    app: phishing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
